// import item from '../../api/circle';
import * as types from '../mutation-types';

const state = {
    // all: [], //列表内容

}

const getters = {
    // activityList: state => state.all,

}

const actions = {
    // getActivityList({ commit }) {
    //     item.getActivity(datas => {
    //         commit(types.activity.RECEIVCE_DATAS, { datas });
    //     })
    // },

}

const mutations = {
    // [types.activity.RECEIVCE_DATAS](state, { datas }) {
    //     state.all = datas;
    //     let now = new Date();
    //     state.running = datas.end_time > now;
    // }, 
}

export default {
    state,
    getters,
    actions,
    mutations
}